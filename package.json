{
  "name": "gas-auto-archive-uby",
  "version": "1.0.0",
  "scripts": {
    "push": "clasp push --force",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
 
